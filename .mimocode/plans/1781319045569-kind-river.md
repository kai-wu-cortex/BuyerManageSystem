# 预览面板优化

## 修改文件
`src/components/supplierQuotes/QuotationArchive.tsx` — PreviewPanel 组件

## 改动点

### 1. 移动端视图优化
- 外层容器: `flex` → `flex flex-col lg:flex-row`
- 左侧文件预览: `w-[45%]` → `h-[40vh] lg:h-auto lg:w-[45%]` (移动端上方占40%高度)
- 右侧详情: `w-[55%]` → `flex-1 lg:w-[55%]`
- 关闭按钮始终可见在右上角

### 2. 币种可编辑
- 币种 input 从 `readOnly` 改为可编辑
- 添加 state `editCurrency` 初始化为 `quotation.currency`
- 保存时一并提交

### 3. 添加税率字段
- 在币种旁添加"税率 %"输入框
- 添加 state `editTaxRate` 初始化为 `quotation.taxRate`
- 保存时一并提交

### 4. 产品名称格式
- 当前代码已正确显示 `item.sourceProductName`（支持任意格式）
- 无需修改，已兼容中文、英文、数字、混合格式

## 验证
- 手机视图：预览面板上下排列，文件在上，详情在下
- 桌面视图：左右分栏不变
- 修改币种/税率后保存，刷新确认持久化
